services:
  redis:
    image: redis:7-alpine
    container_name: rate-limiter-redis
    network_mode: host
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes

volumes:
  redis-data: