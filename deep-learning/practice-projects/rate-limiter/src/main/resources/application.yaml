spring:
  application:
    name: rate-limiter
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 2

app:
  rate-limit:
    max-requests: 2
    window: 5s
